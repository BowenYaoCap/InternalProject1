@model List<InternalProject1.Models.Employee>

<div class ="row">
    <div class="col-md-6">
         <a> 
            @Html.ActionLink("Export to Excel",
            "Export",
            "Home",
            new { @class = "btn btn-primary"}
            )
        </a>
    </div>
    <div class="col-md-6">
        <a> 
            @Html.ActionLink("Import to Excel",
            "Import",
            "Home",
            new { @class = "btn btn-primary"}
            )
        </a>
    </div>
</div>
<div class ="row">
    <div class ="col-md-12">
        <table class="table">
                <tr>
                    <td>Id</td>
                    <td>Name</td>
                    <td>Role</td>
                    <td>Email</td>
                </tr>
            <tbody>
                @foreach (var employee in Model)
                {
                    <tr>
                        <td>@employee.Id</td>
                        <td>@employee.Name</td>
                        <td>@employee.Role</td>
                        <td>@employee.Email</td>
                        <td>@Html.ActionLink("Edit","Edit",new{Id = employee.Id},htmlAttributes: new {@class="btn"}) </td>
                        <td>@Html.ActionLink("Delete","DeleteEmployee",new{Id = employee.Id},htmlAttributes: new {@class="btn"}) </td>
                    </tr>
                }
            </tbody>
        </table>
        @Html.ActionLink("Create New Employee","Add","Home",htmlAttributes: new {@class="btn btn-primary"})
    </div>
</div>
